---
import { Sun, Moon } from '@lucide/astro';

interface Props {
	themeToggleAria: string;
	themeToggleHint: string;
	themeKeyT: string;
}

const { themeToggleAria, themeToggleHint, themeKeyT } = Astro.props;
---

<div class="u-floating-hint" data-anchored-floating-hint>
	<button
		type="button"
		class="icon-btn"
		id="theme-toggle"
		data-theme-toggle
		data-floating-hint-anchor
		aria-label={themeToggleAria}
	>
		<Sun class="theme-icon theme-icon--sun" />
		<Moon class="theme-icon theme-icon--moon" />
	</button>
	<div class="u-floating-hint__layer" data-floating-hint-layer role="tooltip">
		<span class="u-floating-hint__text">{themeToggleHint}</span>
		<kbd class="u-floating-hint__key">{themeKeyT}</kbd>
	</div>
</div>
