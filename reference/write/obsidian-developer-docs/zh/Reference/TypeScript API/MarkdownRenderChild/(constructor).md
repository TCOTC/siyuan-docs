---
aliases: "MarkdownRenderChild.(constructor)"
cssclasses: hide-title
---

<!-- 不要编辑此文件。它由 API Documenter 自动生成。 -->

[`MarkdownRenderChild`](MarkdownRenderChild) › [`(构造函数)`](MarkdownRenderChild/(构造函数).md)

## MarkdownRenderChild.（构造函数）

构造 `MarkdownRenderChild` 类的新实例

**签名：**

```typescript
constructor(containerEl: HTMLElement);
```

## 参数

|  参数|类型 |描述 |
|  --- | --- | --- |
|  <code>containerEl</code> | <code>HTMLElement</code> |该 HTMLElement 将用于测试该组件是否仍然存在。它应该是 Markdown 预览部分的子级，当它不再附加时（例如，由于用户编辑了 Markdown 源代码而被新版本替换时），该组件将被卸载。 |

