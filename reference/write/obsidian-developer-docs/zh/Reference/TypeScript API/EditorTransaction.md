---
aliases: "EditorTransaction"
cssclasses: hide-title
---

<!-- 不要编辑此文件。它由 API Documenter 自动生成。 -->

[`EditorTransaction`](EditorTransaction)

## EditorTransaction接口


**签名：**

```typescript
export interface EditorTransaction 
```

## 属性

|  物业 |修改器 |类型 |描述 |
|  --- | --- | --- | --- |
|  [`更改？`](EditorTransaction/更改) |  | [`EditorChange`](EditorChange)<code>[]</code> | _（可选）_ |
|  [`replaceSelection？`](EditorTransaction/replaceSelection) |  | <code>字符串</code> | _（可选）_ |
|  [`选择？`](EditorTransaction/选择) |  | [`EditorRangeOrCaret`](EditorRangeOrCaret) | _（可选）_ |
|  [`选择？`](EditorTransaction/选择) |  | [`EditorRangeOrCaret`](EditorRangeOrCaret)<code>[]</code> | _（可选）_ 多项选择，覆盖<code>选择</code>。 |

