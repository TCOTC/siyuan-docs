---
aliases: "DataAdapter.trashLocal"
cssclasses: hide-title
---

<!-- 不要编辑此文件。它由 API Documenter 自动生成。 -->

[`DataAdapter`](DataAdapter) › [`trashLocal`](DataAdapter/trashLocal)

## DataAdapter.trashLocal() 方法

移至当地垃圾箱。文件将移至 Vault 根目录下的“.trash”文件夹中。

**签名：**

```typescript
trashLocal(normalizedPath: string): Promise<void>;
```

## 参数

|  参数|类型 |描述 |
|  --- | --- | --- |
|  <code>标准化路径</code> | <code>字符串</code> |文件/文件夹的路径，使用 [normalizePath()](normalizePath) 预先标准化。 |

**退货：**

`承诺<无效>`

