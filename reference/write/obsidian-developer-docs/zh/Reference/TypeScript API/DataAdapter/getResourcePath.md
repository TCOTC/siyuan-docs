---
aliases: "DataAdapter.getResourcePath"
cssclasses: hide-title
---

<!-- 不要编辑此文件。它由 API Documenter 自动生成。 -->

[`DataAdapter`](DataAdapter) › [`getResourcePath`](DataAdapter/getResourcePath)

## DataAdapter.getResourcePath() 方法

返回供浏览器引擎使用的 URI，例如嵌入图像。

**签名：**

```typescript
getResourcePath(normalizedPath: string): string;
```

## 参数

|  参数|类型 |描述 |
|  --- | --- | --- |
|  <code>标准化路径</code> | <code>字符串</code> |文件/文件夹的路径，使用 [normalizePath()](normalizePath) 预先标准化。 |

**退货：**

`字符串`

