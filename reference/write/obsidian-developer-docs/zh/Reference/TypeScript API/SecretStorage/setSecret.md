---
aliases: "SecretStorage.setSecret"
cssclasses: hide-title
---
<!-- 不要编辑此文件。它由 API Documenter 自动生成。 -->

[`SecretStorage`](SecretStorage) › [`setSecret`](SecretStorage/setSecret)

## SecretStorage.setSecret() 方法

在存储中设置一个秘密。

**签名：**

```typescript
setSecret(id: string, secret: string): void;
```## 参数

|  参数|类型 |描述 |
|  --- | --- | --- |
|  <code>id</code> | <code>字符串</code> |带可选破折号的小写字母数字 ID |
|  <code>秘密</code> | <code>字符串</code> |要存储的秘密值 |

**退货：**

`无效`

## 例外情况

如果 ID 无效，则会出现错误

 1.11.4

