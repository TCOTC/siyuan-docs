---
aliases: "Notice.(constructor)"
cssclasses: hide-title
---
<!-- 不要编辑此文件。它由 API Documenter 自动生成。 -->

[`通知`](通知) › [`(构造函数)`](通知/(构造函数).md)

## 注意。（构造函数）

构造`Notice`类的新实例

**签名：**

```typescript
constructor(message: string | DocumentFragment, duration?: number);
```## 参数

|  参数|类型 |描述 |
|  --- | --- | --- |
|  <code>消息</code> | <code>字符串 &#124; </code><code>文档片段</code> |要显示的消息可以是简单的字符串或 |
|  <code>持续时间</code> | <code>号码</code> | _（可选）_ 显示通知的时间（以毫秒为单位）。如果该值为 0，则通知将保持可见，直到用户手动将其关闭。 |

