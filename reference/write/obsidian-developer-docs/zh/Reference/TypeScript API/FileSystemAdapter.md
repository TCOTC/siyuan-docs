---
aliases: "FileSystemAdapter"
cssclasses: hide-title
---

<!-- 不要编辑此文件。它由 API Documenter 自动生成。 -->

[`文件系统适配器`](文件系统适配器)

## 文件系统适配器类

桌面版 Vault 适配器的实现。

**签名：**

```typescript
export class FileSystemAdapter implements DataAdapter 
```
**实现：** [`DataAdapter`](DataAdapter)

## 方法

|  方法|修改器 |描述 |
|  --- | --- | --- |
|  [`追加（规范化路径，数据，选项）`]（FileSystemAdapter /追加）|  |  |
|  [`appendBinary(标准化路径，数据，选项)`](FileSystemAdapter/appendBinary) |  |  1.12.3 |
|  [`复制（标准化路径，标准化新路径）`]（文件系统适配器/复制）|  |  |
|  [`存在（规范化路径，敏感）`]（FileSystemAdapter /存在）|  |  |
|  [`getBasePath()`](FileSystemAdapter/getBasePath) |  |  |
|  [`getFilePath(标准化路径)`](FileSystemAdapter/getFilePath) |  | <p>返回该文件的 file:// 路径</p><p> 0.14.3</p> |
|  [`getFullPath(标准化路径)`](FileSystemAdapter/getFullPath) |  |  |
|  [`getName()`](FileSystemAdapter/getName) |  |  |
|  [`getResourcePath(标准化路径)`](FileSystemAdapter/getResourcePath) |  |  |
|  [`列表（规范化路径）`]（FileSystemAdapter /列表）|  |  |
|  [`mkdir(标准化路径)`](FileSystemAdapter/mkdir) |  |  |
|  [`mkdir(路径)`](FileSystemAdapter/mkdir) | <code>静态</code> |  |
|  [`进程（标准化路径，fn，选项）`]（FileSystemAdapter /进程）|  |  |
|  [`读取（规范化路径）`]（FileSystemAdapter /读取）|  |  |
|  [`readBinary(标准化路径)`](FileSystemAdapter/readBinary) |  |  |
|  [`readLocalFile(路径)`](FileSystemAdapter/readLocalFile) | <code>静态</code> |  |
|  [`删除（规范化路径）`]（FileSystemAdapter /删除）|  |  |
|  [`重命名（标准化路径，标准化新路径）`]（FileSystemAdapter /重命名）|  |  |
|  [`rmdir(标准化路径，递归)`](FileSystemAdapter/rmdir) |  |  |
|  [`stat(标准化路径)`](FileSystemAdapter/stat) |  |  0.12.2 |
|  [`trashLocal(标准化路径)`](FileSystemAdapter/trashLocal) |  |  |
|  [`trashSystem(标准化路径)`](FileSystemAdapter/trashSystem) |  |  |
|  [`写入（规范化路径，数据，选项）`]（FileSystemAdapter /写入）|  |  |
|  [`writeBinary（规范化路径，数据，选项）`]（FileSystemAdapter / writeBinary）|  |  |

