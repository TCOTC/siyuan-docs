---
aliases: "Reference"
cssclasses: hide-title
---
<!-- 不要编辑此文件。它由 API Documenter 自动生成。 -->

[`参考`]（参考）

## 参考接口

指向不同位置的项目的基本接口。

**签名：**

```typescript
export interface Reference 
```## 属性

|  物业 |修改器 |类型 |描述 |
|  --- | --- | --- | --- |
|  [`显示文本？`]（参考/显示文本）|  | <code>字符串</code> | _（可选）_ 如果标题与链接文本不同，则可用，在 <code>[[page name&#124;display name]]</code> 的情况下，这将返回 <code>显示名称</code> |
|  [`链接`]（参考/链接）|  | <code>字符串</code> |链接目的地。 |
|  [`原创`](参考/原创) |  | <code>字符串</code> |包含文档中写入的文本。不适用于发布。 |

