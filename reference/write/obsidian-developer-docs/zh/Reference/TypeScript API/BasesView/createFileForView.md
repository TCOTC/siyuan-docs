---
aliases: "BasesView.createFileForView"
cssclasses: hide-title
---

<!-- 不要编辑此文件。它由 API Documenter 自动生成。 -->

[`BasesView`](BasesView) › [`createFileForView`](BasesView/createFileForView)

## BasesView.createFileForView() 方法

显示具有提供的文件名的文件的新注释菜单，以及可选的修改 frontmatter 的函数。

 1.10.2

**签名：**

```typescript
createFileForView(baseFileName?: string, frontmatterProcessor?: (frontmatter: any) => void): Promise<void>;
```

## 参数

|  参数|类型 |描述 |
|  --- | --- | --- |
|  <code>baseFileName</code> | <code>字符串</code> | _（可选）_ |
|  <code>frontmatterProcessor</code> | <code>(frontmatter: 任意) =>无效</code> | _（可选）_ |

**退货：**

`承诺<无效>`

